# MicroCFO Deployment Guide

Complete guide for deploying MicroCFO to production using Docker.

## Table of Contents
1. [Prerequisites](#prerequisites)
2. [Quick Start](#quick-start)
3. [Production Deployment](#production-deployment)
4. [Environment Configuration](#environment-configuration)
5. [Database Setup](#database-setup)
6. [Security Hardening](#security-hardening)
7. [Monitoring & Maintenance](#monitoring--maintenance)
8. [Troubleshooting](#troubleshooting)

## Prerequisites

### System Requirements
- **OS**: Linux (Ubuntu 20.04+), Windows Server 2019+, or macOS
- **RAM**: Minimum 4GB, Recommended 8GB+
- **Storage**: Minimum 20GB free space
- **CPU**: 2+ cores recommended

### Software Requirements
- Docker 24.0+ and Docker Compose 2.0+
- Git 2.30+
- OpenSSL (for encryption key generation)

### API Keys Required
- **Gemini API Key**: Get from [Google AI Studio](https://makersuite.google.com/app/apikey)
- **OpenAI API Key** (optional): For fallback AI features

## Quick Start

### 1. Clone Repository
```bash
git clone https://github.com/yourusername/microcfo.git
cd microcfo
```

### 2. Configure Environment
```bash
# Copy example environment file
cp .env.example .env

# Edit .env with your configuration
nano .env  # or use your preferred editor
```

### 3. Generate Encryption Keys
```bash
# Run encryption setup
python setup_encryption.py

# This generates:
# - ENCRYPTION_KEY in .env
# - encryption_key.key file (backup this securely!)
```

### 4. Start Services
```bash
# Start all services
docker-compose up -d

# Check service status
docker-compose ps

# View logs
docker-compose logs -f
```

### 5. Verify Deployment
```bash
# Check backend health
curl http://localhost:8000/health

# Check frontend
curl http://localhost/

# Check Flower (Celery monitoring)
curl http://localhost:5555/
```

## Production Deployment

### Step 1: Server Preparation

#### Update System
```bash
sudo apt update && sudo apt upgrade -y
```

#### Install Docker
```bash
# Install Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Install Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Verify installation
docker --version
docker-compose --version
```

#### Configure Firewall
```bash
# Allow HTTP/HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Allow SSH (if needed)
sudo ufw allow 22/tcp

# Enable firewall
sudo ufw enable
```

### Step 2: Application Setup

#### Clone and Configure
```bash
# Clone repository
git clone https://github.com/yourusername/microcfo.git
cd microcfo

# Create production environment file
cp .env.docker .env
```

#### Configure Production Environment
Edit `.env` with production values:

```bash
# Server Configuration
DEBUG=false
HOST=0.0.0.0
PORT=8000

# Database (Use strong passwords!)
POSTGRES_DB=microcfo_prod
POSTGRES_USER=microcfo_admin
POSTGRES_PASSWORD=<STRONG_PASSWORD_HERE>
POSTGRES_PORT=5432

# Redis (Use strong password!)
REDIS_PASSWORD=<STRONG_REDIS_PASSWORD>
REDIS_PORT=6379

# Security (Generate strong secret!)
JWT_SECRET_KEY=<GENERATE_STRONG_SECRET>
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Encryption (Generated by setup_encryption.py)
ENCRYPTION_KEY=<GENERATED_BY_SETUP_SCRIPT>

# CORS (Your production domain)
FRONTEND_URL=https://yourdomain.com

# API Keys
GEMINI_API_KEY=<YOUR_GEMINI_KEY>
OPENAI_API_KEY=<YOUR_OPENAI_KEY>

# Storage (Optional - for S3)
USE_S3_STORAGE=false
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET_NAME=
AWS_REGION=us-east-1

# Monitoring
SENTRY_DSN=  # Optional: For error tracking
```

#### Generate Secure Secrets
```bash
# Generate JWT secret
openssl rand -hex 32

# Generate encryption key
python setup_encryption.py

# Generate strong passwords
openssl rand -base64 32
```

### Step 3: SSL/TLS Setup (HTTPS)

#### Option A: Using Let's Encrypt (Recommended)

Create `docker-compose.prod.yml`:
```yaml
version: '3.8'

services:
  # ... existing services ...

  nginx:
    image: nginx:alpine
    container_name: microcfo-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - certbot-data:/var/www/certbot
    depends_on:
      - frontend
      - backend
    networks:
      - microcfo-network

  certbot:
    image: certbot/certbot
    container_name: microcfo-certbot
    volumes:
      - certbot-data:/var/www/certbot
      - ./nginx/ssl:/etc/letsencrypt
    command: certonly --webroot --webroot-path=/var/www/certbot --email your@email.com --agree-tos --no-eff-email -d yourdomain.com

volumes:
  certbot-data:

networks:
  microcfo-network:
    driver: bridge
```

Create `nginx/nginx.conf`:
```nginx
upstream backend {
    server backend:8000;
}

upstream frontend {
    server frontend:80;
}

server {
    listen 80;
    server_name yourdomain.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com;

    ssl_certificate /etc/nginx/ssl/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/live/yourdomain.com/privkey.pem;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # API endpoints
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket support
    location /ws/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    # Frontend
    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### Obtain SSL Certificate
```bash
# Create nginx directory
mkdir -p nginx/ssl

# Start services
docker-compose -f docker-compose.prod.yml up -d

# Obtain certificate
docker-compose -f docker-compose.prod.yml run --rm certbot

# Reload nginx
docker-compose -f docker-compose.prod.yml restart nginx
```

### Step 4: Database Initialization

```bash
# Run database migrations
docker-compose exec backend alembic upgrade head

# Seed legal database (optional but recommended)
docker-compose exec backend python scripts/seed_downloader.py
docker-compose exec backend python scripts/seed_data.py

# Verify database
docker-compose exec postgres psql -U microcfo_admin -d microcfo_prod -c "\dt"
```

### Step 5: Start Production Services

```bash
# Start all services
docker-compose -f docker-compose.prod.yml up -d

# Check status
docker-compose ps

# View logs
docker-compose logs -f backend
```

## Environment Configuration

### Required Environment Variables

| Variable | Description | Example | Required |
|----------|-------------|---------|----------|
| `GEMINI_API_KEY` | Google Gemini API key | `AIza...` | Yes |
| `JWT_SECRET_KEY` | JWT signing secret | `random-32-char-hex` | Yes |
| `ENCRYPTION_KEY` | Data encryption key | `generated-by-setup` | Yes |
| `POSTGRES_PASSWORD` | Database password | `strong-password` | Yes |
| `REDIS_PASSWORD` | Redis password | `strong-password` | Yes |
| `FRONTEND_URL` | Frontend domain | `https://yourdomain.com` | Yes |
| `DEBUG` | Debug mode | `false` | No |
| `USE_S3_STORAGE` | Enable S3 storage | `true/false` | No |

### Optional Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `OPENAI_API_KEY` | OpenAI API key (fallback) | None |
| `AWS_ACCESS_KEY_ID` | AWS access key | None |
| `AWS_SECRET_ACCESS_KEY` | AWS secret key | None |
| `AWS_S3_BUCKET_NAME` | S3 bucket name | None |
| `SENTRY_DSN` | Sentry error tracking | None |
| `LOG_LEVEL` | Logging level | `INFO` |

## Database Setup

### PostgreSQL Configuration

#### Backup Database
```bash
# Create backup
docker-compose exec postgres pg_dump -U microcfo_admin microcfo_prod > backup_$(date +%Y%m%d).sql

# Restore backup
docker-compose exec -T postgres psql -U microcfo_admin microcfo_prod < backup_20260122.sql
```

#### Database Maintenance
```bash
# Vacuum database
docker-compose exec postgres psql -U microcfo_admin -d microcfo_prod -c "VACUUM ANALYZE;"

# Check database size
docker-compose exec postgres psql -U microcfo_admin -d microcfo_prod -c "SELECT pg_size_pretty(pg_database_size('microcfo_prod'));"
```

### Redis Configuration

#### Monitor Redis
```bash
# Connect to Redis CLI
docker-compose exec redis redis-cli -a <REDIS_PASSWORD>

# Check memory usage
docker-compose exec redis redis-cli -a <REDIS_PASSWORD> INFO memory

# Monitor commands
docker-compose exec redis redis-cli -a <REDIS_PASSWORD> MONITOR
```

## Security Hardening

### 1. Secure Secrets Management

```bash
# Never commit secrets to git
echo ".env" >> .gitignore
echo "encryption_key.key" >> .gitignore

# Backup encryption key securely
cp encryption_key.key /secure/backup/location/

# Set proper file permissions
chmod 600 .env
chmod 600 encryption_key.key
```

### 2. Network Security

```bash
# Restrict database access (docker-compose.yml)
# Remove ports exposure for postgres and redis in production
# Only expose backend and frontend ports
```

### 3. Container Security

```bash
# Run containers as non-root user (already configured in Dockerfile)
# Scan images for vulnerabilities
docker scan microcfo-backend:latest

# Keep images updated
docker-compose pull
docker-compose up -d
```

### 4. Application Security

- Enable rate limiting (configured in middleware)
- Use HTTPS only in production
- Set secure cookie flags
- Implement CORS properly
- Enable audit logging
- Regular security updates

## Monitoring & Maintenance

### Health Checks

```bash
# Backend health
curl https://yourdomain.com/api/health

# Database health
docker-compose exec postgres pg_isready

# Redis health
docker-compose exec redis redis-cli -a <PASSWORD> ping
```

### Log Management

```bash
# View all logs
docker-compose logs -f

# View specific service logs
docker-compose logs -f backend
docker-compose logs -f celery_worker

# Export logs
docker-compose logs --no-color > logs_$(date +%Y%m%d).txt
```

### Celery Monitoring (Flower)

Access Flower dashboard at `http://yourdomain.com:5555`

Features:
- Monitor task execution
- View worker status
- Track task history
- Inspect task details

### Performance Monitoring

```bash
# Container resource usage
docker stats

# Database connections
docker-compose exec postgres psql -U microcfo_admin -d microcfo_prod -c "SELECT count(*) FROM pg_stat_activity;"

# Redis memory
docker-compose exec redis redis-cli -a <PASSWORD> INFO memory
```

### Backup Strategy

#### Automated Backups

Create `backup.sh`:
```bash
#!/bin/bash
BACKUP_DIR="/backups/microcfo"
DATE=$(date +%Y%m%d_%H%M%S)

# Create backup directory
mkdir -p $BACKUP_DIR

# Backup database
docker-compose exec -T postgres pg_dump -U microcfo_admin microcfo_prod | gzip > $BACKUP_DIR/db_$DATE.sql.gz

# Backup volumes
docker run --rm -v microcfo_legal_db:/data -v $BACKUP_DIR:/backup alpine tar czf /backup/legal_db_$DATE.tar.gz -C /data .
docker run --rm -v microcfo_scheme_db:/data -v $BACKUP_DIR:/backup alpine tar czf /backup/scheme_db_$DATE.tar.gz -C /data .

# Backup encryption key
cp encryption_key.key $BACKUP_DIR/encryption_key_$DATE.key

# Remove old backups (keep last 7 days)
find $BACKUP_DIR -name "*.gz" -mtime +7 -delete
find $BACKUP_DIR -name "*.key" -mtime +7 -delete

echo "Backup completed: $DATE"
```

Schedule with cron:
```bash
# Edit crontab
crontab -e

# Add daily backup at 2 AM
0 2 * * * /path/to/backup.sh >> /var/log/microcfo-backup.log 2>&1
```

### Updates and Upgrades

```bash
# Pull latest changes
git pull origin main

# Rebuild images
docker-compose build --no-cache

# Run database migrations
docker-compose exec backend alembic upgrade head

# Restart services
docker-compose up -d

# Verify deployment
curl https://yourdomain.com/api/health
```

## Troubleshooting

### Common Issues

#### 1. Services Won't Start

```bash
# Check logs
docker-compose logs backend

# Check disk space
df -h

# Check memory
free -h

# Restart services
docker-compose restart
```

#### 2. Database Connection Errors

```bash
# Check database is running
docker-compose ps postgres

# Check database logs
docker-compose logs postgres

# Test connection
docker-compose exec backend python -c "from database import engine; print(engine.connect())"
```

#### 3. Redis Connection Errors

```bash
# Check Redis is running
docker-compose ps redis

# Test connection
docker-compose exec redis redis-cli -a <PASSWORD> ping

# Check password in .env matches docker-compose.yml
```

#### 4. Frontend Not Loading

```bash
# Check frontend logs
docker-compose logs frontend

# Check nginx configuration
docker-compose exec nginx nginx -t

# Rebuild frontend
docker-compose build frontend
docker-compose up -d frontend
```

#### 5. Celery Tasks Not Running

```bash
# Check worker status
docker-compose logs celery_worker

# Check Flower dashboard
curl http://localhost:5555/

# Restart workers
docker-compose restart celery_worker celery_beat
```

### Debug Mode

Enable debug mode temporarily:
```bash
# Edit .env
DEBUG=true

# Restart backend
docker-compose restart backend

# View detailed logs
docker-compose logs -f backend
```

### Performance Issues

```bash
# Check resource usage
docker stats

# Increase worker concurrency
# Edit docker-compose.yml:
# command: celery -A celery_app worker --loglevel=info --concurrency=8

# Scale workers
docker-compose up -d --scale celery_worker=3
```

## Support

### Documentation
- [README.md](README.md) - Project overview
- [QUICK_START.md](QUICK_START.md) - Quick start guide
- [SECURITY.md](SECURITY.md) - Security guidelines
- [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md) - Development guide

### Getting Help
- GitHub Issues: Report bugs and request features
- Documentation: Check comprehensive guides
- Logs: Always check logs first

### Useful Commands

```bash
# Quick restart
docker-compose restart

# Full rebuild
docker-compose down -v
docker-compose up -d --build

# Clean everything
docker-compose down -v --remove-orphans
docker system prune -a

# Export configuration
docker-compose config > docker-compose.resolved.yml
```

---

**Deployment Status**: Production-Ready ✅
**Last Updated**: January 22, 2026
**Version**: 2.0.0
